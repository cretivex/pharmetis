/**
 * Design tokens for Buyer app. Use with Tailwind theme (see tailwind.config.js).
 * Prefer theme classes: text-foreground, bg-background, text-muted-foreground, border-border, bg-muted.
 */
export const tokens = {
  role: 'BUYER',
  // Semantic names only; actual values in Tailwind/CSS
  colors: {
    primary: 'primary',
    accent: 'accent',
    background: 'background',
    foreground: 'foreground',
    muted: 'muted',
    mutedForeground: 'muted-foreground',
    border: 'border',
  },
};
