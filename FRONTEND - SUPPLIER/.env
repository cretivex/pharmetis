# Local backend (uncomment for npm run dev against your API):
VITE_API_URL=http://localhost:5000/api
# VITE_API_URL=https://api.pharmetis.in/api
# Must match backend PAYLOAD_SECRET exactly
VITE_PAYLOAD_SECRET=replace_with_a_strong_payload_secret
